# SPDX-FileCopyrightText: Copyright (c) 2024 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: LicenseRef-NvidiaProprietary
#
# NVIDIA CORPORATION, its affiliates and licensors retain all intellectual
# property and proprietary rights in and to this material, related
# documentation and any modifications thereto. Any use, reproduction,
# disclosure or distribution of this material and related documentation
# without an express license agreement from NVIDIA CORPORATION or
# its affiliates is strictly prohibited.

[package]
title = "Omnicool"
version = "0.1.0"  # Semantic Versioning is used: https://semver.org/
description = "The simplest python UI extension example. Use it as a starting point for your extensions."
category = "Example"  # How to categorize this in the Extension Manager
changelog = "docs/CHANGELOG.md"
icon = "data/icon.png"  # Icon to show in the Extension Manager
keywords = ["kit", "example", "extension", "python", "ui"]  # Search keywords for the Extension Manager
preview_image = "data/preview.png"  # Preview to show in the Extension Manager
readme  = "docs/README.md"  # Path (relative to the root) or content of readme markdown file for UI
repository = "https://github.com/NVIDIA-Omniverse/kit-app-template"  # URL of the extension source repository


[dependencies]
"omni.kit.uiapp" = {}


[settings]
exts."omnicool.webapp".autoLaunch = true
exts."omnicool.webapp".openBrowser = true
exts."omnicool.webapp".host = "127.0.0.1"
exts."omnicool.webapp".port = 3001
exts."omnicool.webapp".webRoot = "data/webapp"

# WebSocket USD bridge
exts."omnicool.webapp".wsHost = "127.0.0.1"
exts."omnicool.webapp".wsPort = 8899

# WebRTC signaling server (aiortc — pure Python, no Node.js runtime)
# Set webrtcEnabled = true to start the signaling endpoint on startup.
# Browsers post their SDP offer to http://<webrtcHost>:<webrtcPort>/webrtc/offer
# and receive an SDP answer; the resulting RTCDataChannel uses the same
# JSON message protocol as the WebSocket USD bridge.
exts."omnicool.webapp".webrtcEnabled = false
exts."omnicool.webapp".webrtcHost = "127.0.0.1"
exts."omnicool.webapp".webrtcPort = 8900


[[python.module]]  # Main python module this extension provides, it will be publicly available as "import omnicool.webapp"
name = "omnicool.webapp"


[documentation]
pages = [
    "docs/Overview.md",
    "docs/CHANGELOG.md",
]


[[test]]
dependencies = [
    "omni.kit.test",
    "omni.kit.ui_test"  # UI testing extension
]

args = [
]
